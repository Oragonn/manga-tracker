# 📚 **Manga Tracker – Complete Project Summary (Updated January 2026)**

## 🎯 **Project Overview**

A self-hosted manga/manhwa tracking application that monitors 1,300+ series across multiple sources (MangaDex, Kagane) with manual chapter control, automated checking, bulk operations, activity logging, automated backups, and multi-source support per series.

---

## 🏗️ **Architecture**

### **Tech Stack**
- **Backend**: Python 3.9+, Flask, SQLite (WAL mode)
- **Frontend**: Vanilla JavaScript, Custom CSS (no frameworks)
- **Special**: Selenium for Kagane (Cloudflare bypass)
- **Integrations**: MangaDex API, Kagane scraper, AniList enrichment

### **File Structure**
```
manga-tracker/
├── backend/
│   ├── api.py                    # Flask routes & queue-based adding
│   ├── database.py               # SQLite operations with global lock
│   ├── scheduler.py              # Background chapter checking
│   ├── main.py                   # Additional routes (errors, logs, backups)
│   ├── error_logger.py           # 7-day rotating error logs
│   ├── activity_logger.py        # 14-day activity log with undo
│   ├── backup_manager.py         # Hourly database backups
│   ├── selenium_kagane.py        # Singleton Selenium client
│   └── trackers/
│       ├── mangadex.py           # MangaDex API integration
│       ├── kagane.py             # Kagane scraper (Selenium-based, season-aware)
│       └── anilist.py            # AniList enrichment (covers, titles)
├── data/
│   ├── tracker.db                # Main SQLite database
│   └── last_errors_visit.txt    # Error badge tracking
├── logs/                         # Error logs (7-day rotation)
├── backups/                      # Database backups (7-day retention)
├── web/
│   ├── static/
│   │   ├── css/style.css         # Clean, organized CSS (~600 lines)
│   │   └── js/
│   │       ├── dashboard.js      # Main dashboard logic
│   │       └── backups.js        # Backup management UI
│   └── templates/
│       ├── index.html            # Main dashboard
│       ├── errors.html           # Error log viewer
│       ├── logs.html             # Activity log viewer
│       └── backups.html          # Backup management
└── requirements.txt
```

---

## 🗄️ **Database Schema**

### **Core Tables**

#### `series` - Main series tracking
```sql
CREATE TABLE series (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    title_en TEXT,
    title_romaji TEXT,
    title_native TEXT,
    source_url TEXT NOT NULL UNIQUE,              -- Legacy: kept for backward compatibility
    cover_url TEXT,
    banner_url TEXT,
    status TEXT NOT NULL CHECK(status IN ('reading', 'plan_to_read', 'on_hold', 'dropped', 'completed')),
    source_status TEXT,                            -- Publication status
    source_type TEXT DEFAULT 'other',              -- manga/manhwa/manhua/other
    anilist_id INTEGER,
    alt_titles TEXT,                               -- JSON array
    current_chapter REAL NOT NULL DEFAULT -1,
    current_volume TEXT,
    latest_chapter REAL,
    total_chapters INTEGER DEFAULT 0,
    latest_release DATETIME,
    last_check DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    genres TEXT,                                   -- JSON array
    content_rating TEXT DEFAULT 'unknown',         -- safe/mild/mature/explicit
    searchable_text TEXT                           -- Normalized search index
);
```

#### `series_sources` - Multi-source support (NEW!)
```sql
CREATE TABLE series_sources (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    series_id INTEGER NOT NULL,
    source_url TEXT NOT NULL UNIQUE,
    source_type TEXT NOT NULL,                     -- 'mangadex', 'kagane', etc
    is_primary BOOLEAN DEFAULT 0,                  -- Which source is "main"
    last_check DATETIME,
    added_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (series_id) REFERENCES series(id) ON DELETE CASCADE
);
CREATE INDEX idx_sources_series ON series_sources(series_id);
CREATE INDEX idx_sources_primary ON series_sources(series_id, is_primary);
CREATE UNIQUE INDEX idx_sources_url ON series_sources(source_url);
```

#### `chapters` - Chapter history per series
```sql
CREATE TABLE chapters (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    series_id INTEGER NOT NULL,
    volume TEXT,
    raw_chapter TEXT,
    chapter_number REAL NOT NULL,
    title TEXT,
    release_date DATETIME,
    chapter_url TEXT,
    is_oneshot BOOLEAN DEFAULT 0,
    FOREIGN KEY (series_id) REFERENCES series(id) ON DELETE CASCADE
);
```

#### `activity_log` - User action history with undo
```sql
CREATE TABLE activity_log (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    timestamp DATETIME DEFAULT CURRENT_TIMESTAMP,
    action_type TEXT NOT NULL,                     -- 'added', 'deleted', 'progress', 'status', 'edited'
    series_id INTEGER,                              -- NULL if deleted
    series_title TEXT,
    old_value TEXT,                                 -- JSON: before state
    new_value TEXT,                                 -- JSON: after state
    is_bulk BOOLEAN DEFAULT 0,
    bulk_id TEXT,                                   -- Groups bulk operations
    can_undo BOOLEAN DEFAULT 1,
    FOREIGN KEY (series_id) REFERENCES series(id) ON DELETE SET NULL
);
CREATE INDEX idx_activity_timestamp ON activity_log(timestamp);
CREATE INDEX idx_activity_type ON activity_log(action_type);
CREATE INDEX idx_activity_bulk ON activity_log(bulk_id);
```

#### `meta` - Application metadata
```sql
CREATE TABLE meta (
    key TEXT PRIMARY KEY,
    value TEXT
);
-- Keys: 'last_dashboard_visit', 'schema_version'
```

---

## 🎨 **User Interface**

### **Navigation Header** (All Pages)
```
[Errors] [Dashboard] [Activity Log] [Backups] [Discovery]
     ↑          ↑            ↑           ↑          ↑
  Badge if   Main UI    Undo logs    Manage    External link
  new errors            within 7d    backups
```

### **Dashboard (`/`) - Main Interface**

#### **Control Panel (2 rows)**

**Row 1:**
```
[Status ▾] [Sort ▾] [↕] [↻] [🔔] [Search...]
```
- Status filter (reading/plan/hold/dropped/completed)
- Sort options (unread first/title/release/added/chapters)
- Sort direction toggle
- Reset filters
- Source alerts (placeholder for future)
- Search series

**Row 2:**
```
[Type ▾] [Tags ▾] [Pub Status ▾] [Readable On ▾] | [Actions...]
```
- Content type (manga/manhwa/manhua/other)
- Tags dropdown (combined genre tags + content rating)
- Publication status (ongoing/completed/etc)
- Readable On filter (MangaDex/Kagane)
- Action buttons (Refresh/Add OR Bulk: Read/Edit/Delete)

#### **Series Cards** (221px × 331px covers)
- **Hover**: Checkbox appears (top-right)
- **Click**: Select for bulk operations
- **Selected**: Yellow border (#eab308)
- **Badge**: Red unread count (top-left)
- **Metadata**: Last release time (bottom-left)
- **6 action buttons**: Settings, Google Search, Source, −, +, ✓
- **Continue button**: Links to next unread chapter
- **Middle-click support**: All navigation buttons (Search, Source, Continue) support middle-click to open in background tab

#### **Bulk Mode**
- First selection → enters bulk mode
- Buttons switch: `[Refresh] [+Add]` → `[Read] [Edit] [Delete]`
- All operations exit bulk mode after completion

#### **Series Settings Modal** (NEW!)
Accessible via gear icon on each card. Contains sections:

**1. Sources Management**
- View all sources for a series
- Drag-and-drop to reorder (top = primary)
- Add new sources (MangaDex, Kagane)
- Remove secondary sources
- Primary source used for "Go to Source" button
- Metadata merging: Alt titles and genres from all sources combined

**2. Basic Info**
- Edit title
- Edit cover URL

**3. Reading Progress**
- Change status (reading/plan/hold/dropped/completed)
- Set current chapter (dropdown with all chapters)
- Reset to "Not started"
- Manual "Check Now" button
- Confirm button disabled when no changes made

**4. Actions**
- Delete series (red button, left side)
- Cancel/Save (right side)

### **Activity Log (`/logs`)**
```
[Type Filter ▾] [Time Filter ▾] [Search...]

📖 Read chapter: Solo Leveling
   Ch.45 → Ch.50
   1 hour ago (hover for full date/time)              [Undo]

✏️ Changed status: Reading → On Hold (3 series)
   10 minutes ago                                      [Undo]

🗑️ Deleted 5 series
   • Series A
   • Series B
   • ... and 3 more
   2 minutes ago                                       [Undo]
```
- **Features**: Filter by type/time, search, undo within 7 days
- **Bulk operations**: Grouped with single undo button
- **Auto-cleanup**: Logs deleted after 14 days
- **Hover tooltips**: Show full DD/MM/YYYY HH:MM timestamp on "X ago" text
- **Display improvements**: "Not started" shown for chapter value -1

### **Backups (`/backups`)**
```
📦 Backup Status
• Next backup in: 23 minutes
• Last backup: 2 minutes ago
• Total backups: 168
• Disk usage: 34.2 MB / ~35 MB (7 days)

[↻ Refresh] [➕ Create Backup Now]

🗂️ Available Backups

📅 Today, 6:00 PM
tracker_backup_20241213_180000.db.gz
Size: 2.34 MB  •  2 minutes ago
[⬇️ Download] [↻ Restore]

📅 Today, 5:00 PM
tracker_backup_20241213_170000.db.gz
Size: 2.32 MB  •  1 hour ago
[⬇️ Download] [↻ Restore]
```
- **Features**: View all backups, manual creation, download, restore with warnings
- **Automated**: Hourly backups, 7-day retention, ~35 MB total
- **Safety**: Creates safety backup before restore

### **Errors (`/errors`)**
- View error logs by date
- French timestamp display (Paris timezone)
- "New" badge for unread errors
- 7-day rotation

---

## ⚙️ **Core Features**

### ✅ **Implemented**

1. **Multi-Source Tracking**
   - Multiple sources per series (MangaDex + Kagane simultaneously)
   - User-controlled source management (add/remove/reorder)
   - Primary source designation
   - Automated chapter merging from all sources
   - Metadata merging (alt titles, genres combined)
   - AniList enrichment (covers, titles, genres)
   - Content rating and type classification

2. **Manual Chapter Control**
   - Increment (+), decrement (−), confirm (✓) buttons
   - Volume-aware sorting for manga with volumes
   - Oneshot handling
   - Season-aware parsing for Kagane (handles S2 resets)
   - Confirm button disabled when no changes made

3. **Background Scheduler**
   - Adaptive checking intervals based on status
   - Reading: 30 min, Plan to read: 3h, On hold: 24h, etc.
   - Multi-source chapter scanning
   - Intelligent chapter merging (deduplication by number, keeps most recent)
   - Season offset calculation for multi-season series
   - Graceful error handling and retry logic

4. **Bulk Operations**
   - Select multiple series
   - Mark as read (catch up to latest)
   - Change status (reading/plan/hold/etc)
   - Delete multiple series
   - Activity logging for all bulk actions

5. **Activity Log with Undo**
   - Logs: add, delete, progress, status, edits
   - Undo within 7 days
   - Bulk operation undo (single button)
   - Auto-cleanup after 14 days
   - Source change tracking
   - Hover tooltips showing full timestamp
   - Proper display of "Not started" for chapter -1

6. **Database Backups**
   - Automated hourly backups
   - 7-day retention (~35 MB storage)
   - gzip compression (~70% space savings)
   - Manual backup/restore via web UI
   - Safety backups before restore

7. **Advanced Filtering**
   - Multi-select: Content type, genres, rating, pub status
   - NEW: "Readable On" filter (MangaDex/Kagane sources)
   - Combined Tags dropdown (genres + content rating in same menu)
   - Text search across all titles (including alt titles)
   - Sorting with direction control
   - Persistent filters (no URL params)

8. **Error Logging**
   - 7-day rotating logs
   - French timestamp display
   - "New" badge in navigation
   - Per-date viewing

9. **UI Polish**
   - Back to top button
   - Smooth scrolling
   - Custom scrollbars
   - Responsive design
   - Confirmation modals for destructive actions
   - Drag-and-drop source reordering
   - Refresh button rotation animation
   - Middle-click support on navigation buttons (Search Google, Go to Source, Continue)
   - Fixed pagination margin/padding when only 1 page

---

## 🔧 **Multi-Source Architecture**

### **Overview**
A single series can track chapters from multiple sources simultaneously (e.g., MangaDex + Kagane). This is **user-controlled** - the system never auto-links sources.

### **Why User-Controlled?**
- Series with same name ≠ always same series
- Example: "Solo Leveling" could be:
  - Solo Leveling (manhwa)
  - Solo Leveling (light novel)
  - Solo Leveling (different translations)
- User must confirm which sources belong together

### **Database Design**
Uses a separate `series_sources` table:
```sql
series (1) → (N) series_sources
- Each series can have unlimited sources
- One source is marked as "primary"
- Legacy source_url column kept for backward compatibility
```

### **User Workflows**

#### **Adding a Second Source**
1. Open series settings (gear icon)
2. Click "Add Another Source" button
3. Paste URL (MangaDex or Kagane)
4. System validates URL type
5. Click "Add" → fetches chapters
6. Chapters merged with existing sources
7. Alt titles and genres merged automatically

#### **Reordering Sources (Primary Selection)**
1. Open series settings → Sources section
2. Drag-and-drop sources to reorder
3. Top source = primary source
4. Used for "Go to Source" button on card
5. Changes saved on modal close

#### **Removing a Source**
1. Open series settings → Sources section
2. Click trash icon next to non-primary source
3. Confirm deletion
4. Source removed, chapters remain
5. Cannot remove last source or primary (must reorder first)

### **Chapter Merging Logic**
When a series has multiple sources:

```python
def merge_chapters_from_sources(series_id):
    """Merge chapters from all sources."""
    sources = get_sources_for_series(series_id)
    all_chapters = []
    
    for source in sources:
        chapters = fetch_chapters(source.url)
        for ch in chapters:
            ch['source_id'] = source.id
            ch['source_type'] = source.source_type
        all_chapters.extend(chapters)
    
    # Deduplicate by chapter number (keep most recent)
    unique_chapters = {}
    for ch in all_chapters:
        ch_num = ch['chapter_number']
        if ch_num not in unique_chapters or ch['release_date'] > unique_chapters[ch_num]['release_date']:
            unique_chapters[ch_num] = ch
    
    return sorted(unique_chapters.values(), key=lambda x: x['chapter_number'])
```

**Example:**
- MangaDex: Ch. 1-150 (primary)
- Kagane: Ch. 145-149 (secondary, released earlier)
- Result: Ch. 1-150 (uses Kagane for 145-149 if newer release date)

### **Metadata Merging**
When adding sources:
- **Alt titles**: Combined from all sources (deduplicated)
- **Genres**: Combined from all sources (deduplicated)
- **Searchable text**: Rebuilt with all titles
- **Cover/banner**: Uses primary source (user can manually override)

### **Activity Logging**
Source changes are logged:
```json
{
  "action_type": "edited",
  "series_id": 123,
  "old_value": {
    "sources": [{"url": "...", "type": "MangaDex", "is_primary": true}]
  },
  "new_value": {
    "sources": [
      {"url": "...", "type": "MangaDex", "is_primary": true},
      {"url": "...", "type": "Kagane", "is_primary": false}
    ]
  }
}
```

---

## 🌟 **Special Features**

### **Season-Aware Parsing (Kagane)**
Handles series where seasons reset chapter numbering:

**Problem:**
- Season 1: Episode 1-65
- Season 2: (S2) Episode 1-23

**Solution:**
- Detects season markers: `(S2)`, `[Season 2]`, `{S3}`, etc.
- Calculates offset: Season 2 offset = 65 (last of Season 1)
- Applies offset: `(S2) Episode 1` → Chapter 66
- Result: Continuous numbering (1-88), no data loss

**Supported Formats:**
- Prefixes: `(S2) Episode 3`, `[Season 2] Episode 3`, `S3 Episode 3`
- Suffixes: `Episode 65 (Season 1 Finale)`, `Episode 1 (S2 Premiere)`
- Combined: `(S2) Episode 1 (Season 2 Premiere)`

### **Smart Search**
- Searches across: main title, English title, romaji, native, alt titles
- Normalized matching: removes punctuation, diacritics
- Multi-word support: all words must match (AND logic)

### **Volume-Aware Sorting**
For manga with volumes:
- Chapters sorted by volume first, then chapter number
- Dropdown shows: "Vol.3 Ch.25" instead of just "Ch.25"
- Handles numeric volumes (1, 2, 3) and text volumes

### **Adaptive Checking**
Background scheduler checks series at different intervals:
```python
intervals = {
    'reading': 1800,        # 30 minutes - actively reading
    'plan_to_read': 10800,  # 3 hours - might start soon
    'on_hold': 86400,       # 24 hours - paused
    'dropped': 604800,      # 7 days - rarely check
    'completed': 604800     # 7 days - might get extras
}
```

### **Middle-Click Navigation**
All navigation buttons support middle-click (mouse wheel click):
- **Search Google button**: Opens Google search in background tab
- **Go to Source button**: Opens series source page in background tab
- **Continue button**: Opens next chapter in background tab
- Uses `auxclick` event for proper browser behavior

---

## 📊 **System Statistics**

### **Current Scale**
- **Series tracked**: 1,300+
- **Database size**: ~15-20 MB (uncompressed)
- **Backups**: ~35 MB for 7 days (168 backups)
- **Error logs**: <10 MB for 7 days
- **Activity logs**: ~5 MB for 14 days

### **Performance**
- **Page load**: <500ms (50 series per page)
- **Add series**: 2-5 seconds (MangaDex), 3-7 seconds (Kagane)
- **Background check**: 1-2 seconds per series
- **Backup creation**: 1-2 seconds
- **Database query**: <100ms average
- **Multi-source merge**: <1 second (2-3 sources)

---

## 🔧 **Configuration**

### **Scheduler Intervals**
```python
intervals = {
    'reading': 1800,        # 30 minutes
    'plan_to_read': 10800,  # 3 hours
    'on_hold': 86400,       # 24 hours
    'dropped': 604800,      # 7 days
    'completed': 604800     # 7 days
}
```

### **Backup Settings**
```python
BackupManager(
    backup_interval_hours=1,  # Hourly
    retention_days=7          # 7 days
)
```

### **Log Retention**
- **Error logs**: 7 days
- **Activity logs**: 14 days (with 7-day undo window)
- **Backups**: 7 days

---

## 🎯 **Success Metrics**

✅ All 1,300+ series tracked successfully
✅ Zero data loss with backup system
✅ Multi-source support prevents missing chapters
✅ Season-aware parsing handles complex numbering
✅ Bulk operations save hours of manual work
✅ Activity log provides confidence to experiment
✅ Error tracking catches issues proactively
✅ UI remains responsive under load

---

## 🔄 **Recent Updates (January 2026)**

### **UI/UX Improvements**
- ✅ Refresh button rotation animation when clicked
- ✅ Activity log hover tooltips showing full DD/MM/YYYY HH:MM timestamp
- ✅ Confirm button disabled when no changes made in series settings
- ✅ "Not started" properly displayed for chapter value -1 in activity log
- ✅ Fixed pagination margin/padding showing when only 1 page exists
- ✅ Combined Tags and Content Rating into single dropdown menu
- ✅ Added "Readable On" filter dropdown (MangaDex/Kagane)

### **Middle-Click Navigation Support**
- ✅ Search Google button: Middle-click opens in background tab
- ✅ Go to Source button: Middle-click opens in background tab
- ✅ Continue button: Middle-click opens in background tab
- ✅ Uses `auxclick` event for proper browser behavior
- ✅ Left-click maintains normal behavior (opens and switches to tab)

### **Multi-Source Support** (December 2024)
- ✅ Multiple sources per series (MangaDex + Kagane)
- ✅ User-controlled source management
- ✅ Drag-and-drop source reordering
- ✅ Automated chapter merging
- ✅ Metadata merging (alt titles, genres)
- ✅ Activity logging for source changes

### **Season-Aware Parsing** (December 2024)
- ✅ Detects season markers in chapter titles
- ✅ Calculates offsets for continuous numbering
- ✅ Handles multi-season series (S1, S2, S3...)
- ✅ Preserves all chapters (no data loss)
- ✅ Supports all season format variants

### **Activity Log System** (December 2024)
- ✅ Logs all user actions (add/delete/edit/progress/status)
- ✅ Undo within 7 days
- ✅ Bulk operation undo
- ✅ Auto-cleanup after 14 days
- ✅ Fixed: Bulk delete undo with manually re-added series

### **Database Backup System** (December 2024)
- ✅ Automated hourly backups
- ✅ 7-day retention with auto-cleanup
- ✅ gzip compression (~70% space savings)
- ✅ Web UI for backup management
- ✅ Manual backup/restore via UI
- ✅ Safety backups before restore

---

## 🚀 **Future Enhancements**

### **Priority 1: Completed**
- ✅ Multi-source support
- ✅ Season-aware parsing
- ✅ Source management UI
- ✅ Middle-click navigation support

### **Priority 2: Under Consideration**
- 📋 Sorting by available chapter
- 📋 make -, + decreased/increase when holding the button
- 📋 new progress svg in activity log
- 📋 fix kagane way of detecting if a serie is a manga/manhwa/manhua/other
- 📋 Reading statistics dashboard
- 📋 Reading history timeline


---

## 📝 **Next Chat Reference**

When resuming work on this project, provide:
1. This project summary document
2. Any specific issues or feature requests
3. Console/error logs if debugging

The assistant will have full context to continue development! 🚀